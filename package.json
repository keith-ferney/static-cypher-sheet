{
  "name": "static-deploy",
  "version": "1.0.0",
  "description": "A standalone, browser-based character sheet creator for the Cypher System RPG. This version requires no backend server and stores all data locally in your browser.",
  "main": "app.js",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  }
}
